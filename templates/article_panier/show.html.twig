{% extends 'base.html.twig' %}

{% block title %}ArticlePanier{% endblock %}

{% block body %}
    <h1>ArticlePanier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_panier.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_panier_index') }}">back to list</a>

    <a href="{{ path('app_article_panier_edit', {'id': article_panier.id}) }}">edit</a>

    {{ include('article_panier/_delete_form.html.twig') }}
{% endblock %}
