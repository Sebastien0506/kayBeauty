{% extends 'base.html.twig' %}

{% block h1 %}Modifier Prestation{% endblock %}

{% block body %}
   
{{ form_start(formPrestation) }}
    {{ form_row(formPrestation.nom) }}

    {{ form_row(formPrestation.prix) }}
    {{ form_row(formPrestation.dureMinutes) }}

    {{ form_label(formPrestation.description) }}
    {{ form_widget(formPrestation.description) }}
    {{ form_help(formPrestation.description) }}
    {{ form_errors(formPrestation.description) }}

    {{ form_rest(formPrestation) }}
    <input type="submit" value="Modifier">



{{ form_end(formPrestation) }}

{% endblock %}